#ifndef LINKED_LIST_CYCLE_SORT_H
#define LINKED_LIST_CYCLE_SORT_H

#include <stddef.h>
#include <stdlib.h>
#include <stdio.h>

void print_array(const int *array, size_t size);

/* 8. Heap sort */
void heap_sort(int *array, size_t size);


#endif
